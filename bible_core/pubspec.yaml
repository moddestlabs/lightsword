name: bible_core
description: Core Bible data models, parsing, concordance, and business logic (Flutter-independent)
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.9.1
  xml: ^7.0.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
